# Copy this file to .env.local and paste your NVIDIA API key (never commit .env.local).
# Get a key: https://build.nvidia.com/
NVIDIA_API_KEY=your_nvidia_api_key_here
# Fast default for Vercel (optional — moonshotai/kimi-k2.6 is slower and may timeout)
NVIDIA_MODEL=meta/llama-3.1-8b-instruct
